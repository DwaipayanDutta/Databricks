# Runtime dependencies only. For dev/test tooling see requirements-dev.txt.
# This split keeps the production Docker image lean (no pytest/ruff/black/
# mypy shipped to production).

# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.34.0
starlette==1.4.0

# --- Data validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- HTTP client ---
httpx==0.28.1

# --- Retry / resiliency ---
tenacity==9.0.0

# --- Metrics ---
prometheus-client==0.21.1

# --- Caching (optional, only used if REDIS_URL is set) ---
redis==5.2.1

# --- Observability (optional OpenTelemetry hooks) ---
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0

# --- Env file support ---
python-dotenv==1.2.2
